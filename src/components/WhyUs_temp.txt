'use client';
import { useEffect, useRef } from 'react';
import styles from './Maintenance.module.css';

const items = [
  { title: '?”ê°„ ë¦¬í¬???œê³µ', desc: 'ë§¤ì›” ?¬ì´??ë°©ë¬¸?Â·ì„±ê³??°ì´?°ë? ?•ë¦¬??ë¦¬í¬?¸ë¡œ ê³µìœ ?©ë‹ˆ??' },
  { title: '1?œê°„ ?´ë‚´ ?‘ë‹µ ë³´ì¥', desc: 'ë¬¸ì˜ ?‘ìˆ˜ ???‰ê·  1?œê°„ ?´ë‚´ ?µë?, ê¸´ê¸‰ ?¤ë¥˜??ì¦‰ì‹œ ì²˜ë¦¬?©ë‹ˆ??' },
  { title: 'ì¶”ê? ?”ì²­ ? ì—° ë°˜ì˜', desc: '?´ì˜ ì¤?ë°œìƒ?˜ëŠ” ?ˆë¡œ???”êµ¬?¬í•­??ë³„ë„ ë¹„ìš© ?†ì´ ? ì—°?˜ê²Œ ?€?‘í•©?ˆë‹¤.' },
  { title: '?•ê¸° ë³´ì•ˆÂ·?±ëŠ¥ ?ê?', desc: 'ì£¼ê¸°?ìœ¼ë¡??¬ì´???ë„Â·ë³´ì•ˆÂ·ë§í¬ ?íƒœë¥??ê???ê±´ê°•?˜ê²Œ ? ì??©ë‹ˆ??' },
];

export default function Maintenance() {
  const sectionRef = useRef<HTMLElement>(null);

  useEffect(() => {
    const el = sectionRef.current;
    if (!el) return;
    const observer = new IntersectionObserver(
      entries => entries.forEach(e => { 
        if (e.isIntersecting) {
          // ?„ì—­ fade-up ?´ë˜?¤ìš©
          if (e.target.id === 'maintenance') e.target.classList.add('visible');
          // ê°œë³„ ?„ì´?œìš© ì»¤ìŠ¤?€ ? ë‹ˆë©”ì´???´ë˜??ì¶”ê?
          else e.target.classList.add(styles.visible);
        } else {
          // ë·°í¬?¸ì—??ë²—ì–´?˜ë©´ ?´ë˜???œê±°?˜ì—¬ ?¬ì§„?????¤ì‹œ ? ë‹ˆë©”ì´???¸ë¦¬ê±?          if (e.target.id === 'maintenance') e.target.classList.remove('visible');
          else e.target.classList.remove(styles.visible);
        }
      }),
      { threshold: 0.1 }
    );
    observer.observe(el);

    // ê°œë³„ ?„ì´?œë“¤??ê´€ì°°í•˜???œì°¨??? ë‹ˆë©”ì´???˜ë„ë¡?ì²˜ë¦¬
    const animItems = el.querySelectorAll(`.${styles.item}`);
    animItems.forEach(item => observer.observe(item));

    return () => observer.disconnect();
  }, []);

  return (
    <section ref={sectionRef} className={`${styles.section}`} id="maintenance">
      {/* ?™ì  ë°°ê²½ ?¨ê³¼ */}
      <div className={styles.bgBlob} />
      <div className={styles.bgBlob2} />

      <div className={styles.container}>
        <div className={styles.grid}>
          {/* Left: Text */}
          <div className={styles.left}>
            <div className={`section-eyebrow section-eyebrow-white`}>MAINTENANCE</div>
            <h2 className={styles.title}>
              ?œì‘?€ ?œì‘,<br />ê´€ë¦¬ê? ë³¸ì§ˆ?…ë‹ˆ??            </h2>
            <p className={styles.sub}>
              ?€ë¶€ë¶„ì˜ ?…ì²´???©í’ˆê³??¨ê»˜ ê´€ê³„ê? ?ë‚©?ˆë‹¤.<br />
              ?€?¬ëŠ” ê·¸ë•Œë¶€??ì§„ì§œ ?ŒíŠ¸?ˆì‹­???œì‘?œë‹¤ê³??ê°?©ë‹ˆ??
            </p>
          </div>

          {/* Right: Service Items */}
          <div className={styles.right}>
            {items.map((item, i) => (
              <div
                key={i}
                className={styles.item}
                style={{ animationDelay: `${i * 0.15}s` }}
              >
                <div className={styles.checkIcon}>
                  <svg width="24" height="24" viewBox="0 0 20 20" fill="none">
                    <path d="M6 10l3 3 5-5" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                </div>
                <div>
                  <h4 className={styles.itemTitle}>{item.title}</h4>
                  <p className={styles.itemDesc}>{item.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}

