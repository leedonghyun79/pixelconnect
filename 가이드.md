# pixelconnect 프로젝트 구조 가이드

Next.js App Router 기준. 나중에 구조 까먹었을 때 이 문서만 보면 "어디에 뭘 놔야 하는지" 바로 감 잡을 수 있게 정리.

## 폴더 이름에 붙는 특수 문자 (Next.js 라우팅 컨벤션)

| 표기 | 이름 | 역할 | URL 영향 |
|---|---|---|---|
| `_folder` | Private Folder | 라우팅에서 완전히 제외. 컴포넌트/유틸 등 내부 구현을 안전하게 콜로케이션하는 용도 | 없음 (URL로 접근 불가) |
| `[param]` | Dynamic Segment | URL의 가변 구간을 파라미터로 받음 | `/column/[id]` → `/column/abc123` |
| `[...param]` | Catch-all | 1개 이상, 몇 단계든 다 받아서 배열로 넘김 | `/docs/[...slug]` → `/docs/a/b/c` |
| `[[...param]]` | Optional Catch-all | 0개 이상 (아예 없어도 매칭) | pixelconnect엔 미사용 |
| `(group)` | Route Group | 폴더 그룹핑/레이아웃 분기용. URL엔 안 나타남 | 없음 |

pixelconnect는 라우트가 6개뿐이고 레이아웃이 하나라 **route group `()`은 아직 안 씀**. 나중에 로그인 전/후처럼 레이아웃 자체가 완전히 달라지는 섹션(예: `/admin`)이 생기면 그때 `(site)` / `(admin)`으로 나누는 걸 고려.

## 컴포넌트는 어디에 두나 — 4단계 기준

**"몇 군데서 쓰이는가"** 하나로 판단한다.

```
src/components/common/      ← layout.tsx에서 직접 쓰는 것 (모든 페이지에 무조건 등장)
src/components/<Name>/      ← layout엔 없지만 여러 라우트에서 재사용
app/_components/<Name>/     ← 홈(app/page.tsx) 전용 섹션
app/<route>/_components/<Name>/  ← 그 라우트 하나에서만 쓰는 컴포넌트
```

| 위치 | 기준 | 현재 들어있는 것 |
|---|---|---|
| `src/components/common/` | `app/layout.tsx`가 직접 import | Navbar, Footer, SmoothScroll, DevNotice |
| `src/components/` (common 제외) | layout엔 없지만 2개 이상 라우트에서 재사용 | PageTitleBanner, Stats, Portfolio, FinalCTA, FlowWave3D, TurnstileWidget |
| `app/_components/` | `app/page.tsx`(홈)에서만 사용 | Hero, PainPoint, WhyUs, Process, Testimonials, Pricing, FAQ, GridWaveCanvas, SectionGridBg, Maintenance |
| `app/<route>/_components/` | 해당 라우트의 `page.tsx`에서만 사용 | `column/_components/ColumnList`, `column/[id]/_components/HighlightCode`, `contact/_components/ContactForm`, `reviews/_components/ReviewList`, `services/_components/ServiceDetail` |

새 컴포넌트를 만들 때 자문할 것:
1. `layout.tsx`에 직접 넣나? → `components/common/`
2. 2개 이상 라우트에서 재사용하나? → `components/<Name>/`
3. 홈페이지에서만 쓰나? → `app/_components/<Name>/`
4. 특정 라우트 하나에서만 쓰나? → `app/<그 라우트>/_components/<Name>/`

## 컴포넌트는 폴더 단위로 묶는다

```
Hero/
  Hero.tsx
  Hero.module.css
```

파일 탐색기에서 한 컴포넌트의 구성 요소(로직 + 스타일)가 한눈에 보이도록, `Component.tsx`와 `Component.module.css`를 같은 이름의 폴더 안에 넣는다.

## 페이지 자체 스타일은 `page.module.css`

```
column/
  page.tsx
  page.module.css   ← column 목록 페이지 전용 스타일
  _components/
    ColumnList/
```

`page.tsx`가 직접 쓰는 스타일은 라우트 폴더 이름을 따로 붙이지 않고 `page.module.css`로 통일 → "이 css는 이 page.tsx 것"이라는 게 이름만 보고 바로 파악됨.

## 그 외 특수 파일

- `app/robots.ts`, `app/sitemap.ts` — Next.js 전용 메타데이터 파일 컨벤션. `/robots.txt`, `/sitemap.xml` 자동 생성
- `app/feed.xml/route.ts` — RSS 피드. 전용 컨벤션이 없어서 폴더 이름 자체를 `feed.xml`로 만들고 그 안에 `route.ts`(API 핸들러)를 둬서 `/feed.xml` URL을 만드는 방식
- `app/not-found.tsx` — 404 페이지 (Next.js 예약 파일명)

## 현재 전체 구조 스냅샷

```
src/
  app/
    layout.tsx, page.tsx, globals.css
    robots.ts, sitemap.ts, feed.xml/route.ts, not-found.tsx
    _components/              ← 홈 전용
      Hero/ PainPoint/ WhyUs/ Process/ Testimonials/ Pricing/ FAQ/
      GridWaveCanvas/ SectionGridBg/ Maintenance/  (Hero·WhyUs가 내부적으로 씀)
    column/
      page.tsx, page.module.css
      _components/ColumnList/
      [id]/
        page.tsx, page.module.css
        _components/HighlightCode/
    contact/
      page.tsx, page.module.css
      _components/ContactForm/
    portfolio/
      page.tsx
      [slug]/page.tsx, page.module.css
    reviews/
      page.tsx, page.module.css
      _components/ReviewList/
    services/
      page.tsx
      _components/ServiceDetail/
  components/
    common/            ← layout.tsx 전용
      Navbar/ Footer/ SmoothScroll/ DevNotice/
    PageTitleBanner/ Stats/ Portfolio/ FinalCTA/ FlowWave3D/ TurnstileWidget/
  lib/                 ← columns.ts, inquiry.ts (서버 로직)
  data/                ← portfolio.ts (정적 데이터)
```
